<h1>All tags</h1>
<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag[:id]) %>
      <%= link_to "Delete", tag_path(tag[:id]), method: :delete, data: {confirm: "Really delete?"} %>
    </li>
  <% end %>
</ul>

<%= link_to "<< Back to Articles List", articles_path %>
